{
  "name": "frontendmentor",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "start": "parcel packages/*/src/index.html --open",
    "build": "parcel build packages/*/src/index.html --dist-dir dist"
  },
  "devDependencies": {
    "parcel": "2.14.0"
  }
}
